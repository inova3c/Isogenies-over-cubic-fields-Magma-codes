load "gl2data.txt";
finelist:=[Integers()|];
GG:=GL(2, Integers(4));



for i in [1 .. 727] do
	H:=newsublist[i][3];
	if CoefficientRing(H) ne Integers(2) then
		Append(~finelist, i);
	end if;
end for;


order12list:=[Integers()|];
for i in finelist do
	H:=newsublist[i][3];
	H1:=ChangeRing(H, Integers(4));
	if Order(H1) eq 12 then 
		Append(~order12list, i);
	end if; 
end for;

order6list:=[Integers()|];
for i in finelist do
	H:=newsublist[i][3];
	H1:=ChangeRing(H, Integers(4));
	if Order(H1) eq 6 then 
		Append(~order6list, i);
	end if; 
end for;

finallist:=[Integers()|];
for T in Subgroups(newsublist[99][3]) do
	T;
end for;

for i in index12list do 
	H:=newsublist[i][3];
	H1:=ChangeRing(H, Integers(4));
	for T in Subgroups(H1) do
		
		if Order(T[3]) eq 4 then
			for S in Subgroups(BorelSubgroup(Integers(4))) do
				if IsConjugate(GG, S[3], T[3) eq 1 then
					Append(~finallist, i);
				end if;
			end for;
		end if;
	end for;
end for;



if CoefficientRing(H) eq Integers(2) then
	print 1;
end if;

za podgrupu s liste, želim ju prebaciti na mod 4, tj. gledati mod 4 reprezentaciju


nakon prebacivanja, tražim one koje imaju indeks 12 (red 8),
i koje imaju podgrupu reda 4, koja je konjugirana podgrupi gornjetrokutastih mod 4

ako to vrijedi, želim spremiti indeks u novu listu